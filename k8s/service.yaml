apiVersion: v1
kind: Service
metadata:
  name: agentic-system
  labels:
    app: agentic-system
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: agentic-system
